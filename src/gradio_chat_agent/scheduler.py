# TODO: Not completed yet

# def scheduler_tick():
#     now = datetime.utcnow()
#     for sched in repo.due_schedules(now):
#         execute_as_system(
#             project_id=sched.project_id,
#             action_id=sched.action_id,
#             inputs=json.loads(sched.inputs_json),
#         )
